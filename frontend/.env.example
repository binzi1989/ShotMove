# 开发时一般不需要：前端 npm run dev 会通过 Vite 把 /api 代理到 http://127.0.0.1:8000
# 若请求 /api 出现 404，请先确认：1) 后端已启动（在 backend 目录执行 python run.py，端口 8000）2) 前端用 npm run dev 启动
# 若前端不是用 npm run dev（例如单独用其他服务器起静态资源），可取消下行注释并改为你的后端地址
# VITE_API_BASE=http://localhost:8000
